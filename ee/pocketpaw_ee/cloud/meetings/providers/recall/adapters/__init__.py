# Meeting provider connector adapters — Zoom + Google Meet + the
# cross-provider aggregator. Each implements the OSS ConnectorProtocol
# (pocketpaw.connectors.protocol) and is constructed per-workspace by
# pocketpaw_ee.cloud.meetings.service.
